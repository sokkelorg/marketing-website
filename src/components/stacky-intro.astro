<section id="stacky-intro">
  <div class="content">
    <img class="stacky" alt="Stacky." src="/assets/stacky.svg" />
    <div class="speech-bubble">
      <p class="message">
        Hi <span aria-hidden="true">👋</span> I'm Stacky, your AI-powered Software
        Delivery sidekick. I keep your setup clean, tuned, and running like it should.
        Talk to me any time – and I'll ping you when something actually needs your
        attention. You build, I've got the rest.
      </p>
    </div>
  </div>
</section>

<style>
  #stacky-intro {
    display: flex;
    justify-content: center;
    max-width: 24rem;

    .content {
      display: flex;
      flex-direction: column;
      gap: calc(var(--spacing) * 6);
      align-items: center;

      .stacky {
        max-width: 50%;
      }

      .speech-bubble {
        display: flex;
        align-items: center;
        flex-direction: column;

        &::before {
          content: url('/assets/speech-bubble-arrow.svg');
          height: 0.625rem;
        }

        .message {
          margin: 0;
          padding-inline: calc(var(--spacing)*4);
          padding-block: calc(var(--spacing)*3);
          border-radius: var(--radius-md);
          background-color: var(--color-gray-100);
          font-size: var(--text-sm-font-size);
          line-height: var(--text-sm-line-height);
        }
      }
    }
  }
</style>
