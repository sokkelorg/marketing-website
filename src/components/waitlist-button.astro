---
import LinkButton from './link-button.astro';
import { waitlistUrl } from '../waitlist';

export type Props = { wide?: true; inverted?: true };

const { wide, inverted } = Astro.props;
---

<LinkButton href={waitlistUrl} wide={wide} inverted={inverted}
  >Join waitlist</LinkButton
>
