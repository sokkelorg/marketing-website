---
import LinkButton from './link-button.astro';
import { waitlistUrl } from '../waitlist';

export type Props = { wide?: true };

const { wide } = Astro.props;
---

<LinkButton href={waitlistUrl} wide={wide}>Join waitlist</LinkButton>
