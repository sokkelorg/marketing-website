---
import WaitlistButton from './waitlist-button.astro';
---

<section id="hero">
  <h1 class="headline">See what matters.<br />Ship without friction.</h1>
  <p class="subhead">
    Stop wasting cycles on coordination and conflicting features. Sokkel
    connects your stack to automatically flag blockers, process debt, and goal
    misalignment–radically reducing the coordination tax so teams can focus on
    impact.
  </p>
  <WaitlistButton />
</section>

<style>
  #hero {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing) * 8);
    align-items: center;
    text-align: center;

    .headline {
      margin: 0;
      font-weight: var(--font-weight-extrabold);
      line-height: 100%;
      font-size: var(--text-6xl-font-size);
      @media (min-width: 60rem) {
        font-size: var(--text-8xl-font-size);
      }

      color: transparent;
      background-image: url('/assets/flow-title.webp');
      background-repeat: no-repeat;
      background-size: calc(150% + 20px) calc(150% + 20px);
      background-position: -10px -10px;
      background-clip: text;

      @media (prefers-reduced-motion: reduce) {
        background-image: none;
        color: var(--color-text-primary);
      }
    }

    .subhead {
      max-width: 50ch;
      text-wrap: pretty;
      margin-block: 0;
      margin-inline: calc(var(--spacing) * 4);

      font-size: var(--text-lg-font-size);
      line-height: var(--text-lg-line-height);
      font-weight: var(--font-weight-medium);
      @media (min-width: 60rem) {
        font-size: var(--text-xl-font-size);
        line-height: var(--text-xl-line-height);
      }
    }
  }
</style>
