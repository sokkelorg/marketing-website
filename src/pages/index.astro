---
import Layout from '../layouts/Layout.astro';
import Header from '../components/header.astro';
import WaitlistButton from '../components/waitlist-button.astro';
import Footer from '../components/footer.astro';

type PainSource = { title: string; url: string };
type Pain = { title: string; body: string; sources: PainSource[] };

const pains: Pain[] = [
  { title: 'Platform-talent crunch', body: '37 % of firms still can\'t hire the DevOps skills they need, stalling roadmaps and inflating salaries.', sources: [{ title: 'IT Pro', url: 'todo' }] },
  { title: 'Cloud-cost black hole', body: 'Hours disappear hunting idle services; 21–50 % of spend is pure waste for 78 % of companies.', sources: [{ title: 'Tech Monitor', url: 'todo' }, { title: 'BetaNews', url: 'todo'}] },
  { title: 'Security roulette', body: 'One breach can wipe you out; SMEs face an average hit of $3.3 M per incident.', sources: [{ title: 'newsroom.ibm.com', url: 'todo' }] },
  { title: 'Tool-chain time sink', body: 'Nearly 40 % of engineering time vanishes maintaining internal & delivery tools instead of shipping features.', sources: [{ title: 'DZone', url: 'todo' }] },
];
---

<Layout
  schema={[
    {
      '@context': 'https://schema.org',
      '@type': 'WebSite',
      name: 'Sokkel',
      alternateName: ['Sokkel', 'Sokky', 'sokkel.io'],
      url: 'https://sokkel.io/',
    },
    {
      '@context': 'https://schema.org',
      '@type': 'Organization',
      name: 'Sokkel',
      url: 'https://sokkel.io/',
      logo: 'https://sokkel.io/assets/logo.svg',
      sameAs: ['https://www.linkedin.com/company/sokkel'],
      image: 'https://sokkel.io/assets/team.jpg',
      description:
        'Sokkel is the foundation and building blocks for managing your applications, smooooothly.',
      telephone: '+4795308087',
      email: 'hello@sokkel.io',
      address: {
        '@type': 'PostalAddress',
        streetAddress: 'Arbins gate 11',
        addressLocality: 'Oslo',
        addressCountry: 'NO',
        addressRegion: 'Oslo',
        postalCode: '0253',
      },
      vatID: 'NO935108500',
      iso6523Code: '0192:935108500',
    },
  ]}
>
  <Header link={{ text: 'About us', href: '/about-us' }} />

  <main>
    <section id="hero">
      <h1>Effortless<br>software<br>delivery</h1>
      <h2>
        The AI-powered <span aria-hidden="true">✨</span> platform that turns cloud complexity into clarity.
      </h2>
      <WaitlistButton />
    </section>

    <img
      class="lambo"
      src="/assets/burning-car-with-duct-tape.gif"
      alt="Burning Lamborghini with duct tape."
    />

    <section id="tagline">
      <p>
        Simple setups. Always optimized. Change-aware and insight-driven. A new, worryless standard for software delivery.
      </p>
      <p class="smooth">smoooooooooooooooooooooooooooooooooooooooth.</p>
    </section>

    <section id="mission" class="card pitch">
      <h2 class="title">Our mission is simple</h2>
      <p class="body">
        To set a new, worryless standard for software delivery — with simple setups, always-optimized, insight-driven, and change-aware environments, powered by AI.
      </p>
      <WaitlistButton />
    </section>

    <section id="pains">
      <h2>Software delivery today? Kinda painful.</h2>
      {pains.map(pain => (
        <div class="card pain">
          <h3 class="title">{pain.title}</h3>
          <p class="body">{pain.body}</p>
          <div class="sources">
            {pain.sources.map(link => (
              <a href={link.url} rel="noopener noreferrer">{link.title}</a>
            ))}
          </div>
        </div>
      ))}
    </section>

    <section class="comic">
      <div class="setup">
        <h2>Let us tell you a story we hear way too often…</h2>
        <p>The story about everything getting in the way of what you actually wanted to build.</p>
      </div>
      <div class="strip">
        <div class="panel">
          <img alt="todo" src="/assets/comic-panel-1.png" />
          <div class="description">
            <p class="title">This is you, creating your best software ever.</p>
            <p>You're in the flow. Focused. The balloon's right there – everything's working.</p>
          </div>
        </div>
        <div class="panel">
          <img alt="todo" src="/assets/comic-panel-2.png" />
          <div class="description">
            <p class="title">But then the real world starts creeping in…</p>
            <p>Meetings. Roadmaps. Distractions. Your focus slips – the software drifts.</p>
          </div>
        </div>
        <div class="panel">
          <img alt="todo" src="/assets/comic-panel-3.png" />
          <div class="description">
            <p class="title">And then it hits the fan.</p>
            <p>Infra's on fire. You're no longer building – you're firefighting.</p>
          </div>
        </div>
        <div class="panel">
          <img alt="todo" src="/assets/comic-panel-4.png" />
          <div class="description">
            <p class="title">Now you're here. Stuck in migration hell.</p>
            <p>Everything's tangled. Progress is gone. And your dream? Floating far, far away.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="comic tight">
      <div class="setup">
        <h2>So, let's tell the story again.</h2>
        <p>Only with Sokkel and Stacky in the mix.</p>
      </div>

      <div class="strip">
        <div class="panel">
          <p class="title">This is still you, creating your best software ever.</p>
          <img alt="todo" src="/assets/comic-panel-1.png" />
        </div>
        <div class="panel">
          <img alt="todo" src="/assets/comic-panel-5.png" />
        </div>
        <div class="panel">
          <img alt="todo" src="/assets/comic-panel-6.png" />
        </div>
        <div class="panel">
          <img alt="todo" src="/assets/comic-panel-7.png" />
          <p>
            Only now, you're not fighting the cloud to do it. With Sokkel and Stacky, setup is simple, changes are safe, and everything just flows. No fires. No blockers. Just smooth software delivery – every time.
          </p>
        </div>
      </div>
    </section>

    <section id="promise" class="card pitch">
      <img alt="Sokkel." src="/assets/logo.svg">
      <h2 class="text">Not just a platform. A promise. Effortless. Worryless. Smooooth.</h2>
      <WaitlistButton />
    </section>

    <section id="status" class="card">
      <h2><span aria-hidden="true">🟢</span> Building mode</h2>
      <p>We are currently building, testing, and validating the beta version of Sokkel and onboarding pilots.</p>
      <WaitlistButton wide={true} />
    </section>
  </main>

  <Footer
    links={[
      { href: '/about-us', text: 'About us' },
    ]}
    }
  />
</Layout>

<style>
  main {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing)*32);
  }

  #hero {
    display: grid;
    gap: calc(var(--spacing)*8);

    h1 {
      margin: 0;
      font-weight: var(--font-weight-extrabold);
      font-size: var(--text-5xl-font-size);
      line-height: var(--text-5xl-line-height);
    }

    h2 {
      margin: 0;
      font-weight: var(--font-weight-medium);
      font-size: var(--text-xl-font-size);
      line-height: var(--text-xl-line-height);
    }
  }


  .lambo {
    height: 226px;
  }

  #tagline {
    p {
      margin: 0;
    }

    .smooth {
      background: linear-gradient(270deg, #CCCCFF 0%, #9999FF 13%, #6666FF 25%, #3333FF 38%, #0000FF 50%, #0000CC 63%, #000099 76%, #000066 88%, #000033 100%);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
    }
  }


  .card {
    border-radius: var(--radius-2xl);
    padding: calc(var(--spacing)*6);
  }

  .pitch {
    max-height: 500px; /* The height of the background animation. */
    padding-block: calc(var(--spacing)*32);

    background-position: center;
    background-repeat: no-repeat;
    background-color: var(--color-blue-200);
    text-align: center;
    color: var(--color-base-background);

    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing)*6);
    align-items: center;

    .title {
      margin: 0;
      font-weight: var(--font-weight-normal);
      font-size: var(--text-base-font-size);
      line-height: var(--text-base-line-height);
    }

    .body {
      margin: 0;
      font-weight: var(--font-weight-bold);
      font-size: var(--text-2xl-font-size);
      line-height: var(--text-2xl-line-height);
    }
  }

  #mission {
    background-image: url(/assets/flow-2.gif);
  }

  #pains {
    h2 {
      font-weight: var(--font-weight-extrabold);
      font-size: var(--text-2xl-font-size);
      line-height: var(--text-2xl-line-height);
    }

    display: flex;
    flex-direction: column;;
    gap: 1rem;

    .pain {
      display: flex;
      flex-direction: column;
      gap: calc(var(--spacing)*2);
      background-color: var(--color-destructive-foreground);

      .title {
        margin: 0;
        font-weight: var(--font-weight-bold);
      }

     .body {
        margin: 0;
      }

      .sources {
        display: flex;
        gap: calc(var(--spacing)*2);

        a {
          width: fit-content;
          font-weight: var(--font-weight-semibold);
          padding: calc(var(--spacing)*0.5) calc(var(--spacing)*2);
          border-radius: var(--radius-md);
          background-color: var(--color-base-primary);
          color: var(--color-base-primary-foreground);
          text-decoration: none;
          font-size: var(--text-xxs-font-size);
          line-height: var(--text-xxs-line-height);
          text-transform: uppercase;
        }
      }
    }
  }

  .comic {
    display: grid;
    gap: calc(var(--spacing) * 32);

    .setup {
      text-align: center;

      h2 {
        font-weight: var(--font-weight-extrabold);
        font-size: var(--text-4xl-font-size);
        line-height: var(--text-4xl-line-height);
      }

      p {
        color: var(--color-base-muted-foreground);
      }
    }

    .strip {
      display: grid;
      gap: calc(var(--spacing) * 8);

      .panel {
        display: grid;
        gap: calc(var(--spacing) * 4);

        img {
          border: 3px solid var(--color-base-black);
        }

        p {
          margin: 0;
        }

        .title {
          font-weight: var(--font-weight-bold);
        }
      }
    }
  }

  .comic.tight {
    .strip {
      gap: 0;

      .panel:not(:first-of-type) {
        img {
          border-top-width: 0;
        }
      }
    }
  }

  .stacky {
    display: grid;
    margin: 0 auto;

    .body, .eyes {
      grid-column: 1;
      grid-row: 1;
    }

    .eyes {
      position: relative;
      max-width: 35%;
      top: 30%;
      justify-self: center;
    }
  }

  .smile {
    background-color: hsl(240, 100%, 70%);
  }

  #promise {
    background-image: url(/assets/flow-3.gif);
    background-blend-mode: color-burn;
  }

  #status {
    border: 0.125rem hsl(0, 0%, 90%) solid;
  }
</style>
