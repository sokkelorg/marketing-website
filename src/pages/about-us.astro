---
import Layout from '../layouts/Layout.astro';
import Header from '../components/header.astro';
import ContactSection from '../components/contact-section.astro';
import Footer from '../components/footer.astro';
import { Picture } from 'astro:assets';
import theTeam from '../assets/team.jpg';
import { waitlistUrl } from '../waitlist';
---

<Layout>
  <Header links={[
    { href: '/', text: 'Close', icon: 'close' },
    { href: waitlistUrl, text: 'Join waitlist', icon: 'arrow', wideOnly: true },
  ]} />
  <main>
    <section>
      <h1>Hello <span aria-hidden="true">👋</span></h1>

      <Picture
        class="team"
        src={theTeam}
        layout="full-width"
        formats={['avif', 'webp']}
        alt="The team: Marius, Simen, and Martin."
      />

      <p>
        This is us: Marius, Simen, and Martin. Three builders. One designer. Two
        developers.
      </p>

      <p>
        Between us, we’ve been at Finn.no, No Isolation, Auk, AYR, Folio, Bjerk
        and SpotOn. We’ve seen what works, what doesn’t, and what should exist
        but doesn’t (yet).
      </p>

      <p class="emphasis">So we're building Sokkel!</p>
    </section>
    <ContactSection />
  </main>
  <Footer
    links={[
      { href: waitlistUrl, text: 'Join waitlist' },
    ]}
  />
</Layout>

<style>
  main {
    display: grid;
    gap: calc(var(--spacing)*12);

    section {
      display: grid;
      gap: calc(var(--spacing)*6);

      h1 {
        margin: 0;
        font-size: 2rem;
        font-weight: 800;
        line-height: 1.5rem;
      }

      p {
        margin: 0;
      }

      .emphasis {
        font-weight: var(--font-weight-medium);
      }
    }


    .team {
      border-radius: 0.5rem;
    }
  }
</style>
